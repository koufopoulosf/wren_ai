# =============================================================================
# Wren AI Data Assistant - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Anthropic API (Claude - Required for Wren AI text-to-SQL and explanations)
# -----------------------------------------------------------------------------
# Get your API key from: https://console.anthropic.com/
# This is the ONLY API key you need - everything else runs locally!
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: Claude model to use (default: claude-sonnet-4-20250514)
# ANTHROPIC_MODEL=claude-sonnet-4-20250514

# -----------------------------------------------------------------------------
# Vector Search Configuration (Auto-configured for Docker Compose)
# -----------------------------------------------------------------------------
# Qdrant vector database for semantic search
# QDRANT_HOST=qdrant
# QDRANT_PORT=6333

# Ollama for local embeddings (no API key needed!)
# OLLAMA_URL=http://ollama:11434
# EMBEDDING_MODEL=nomic-embed-text
# The embedding model will be auto-downloaded on first start (~270MB)

# Vector collection name
# VECTOR_COLLECTION=schema_entities

# -----------------------------------------------------------------------------
# Database (Pre-configured for Docker Compose PostgreSQL)
# -----------------------------------------------------------------------------
# DB_TYPE=postgres
# DB_HOST=postgres
# DB_PORT=5432
# DB_DATABASE=analytics
# DB_USER=wren_user
# DB_PASSWORD=wren_password
# DB_SSL=false

# -----------------------------------------------------------------------------
# Display Settings
# -----------------------------------------------------------------------------
# Maximum rows to show in UI
# MAX_ROWS_DISPLAY=100

# Maximum rows for CSV/JSON export
# MAX_ROWS_EXPORT=10000

# Enable export formats
# ENABLE_CSV_EXPORT=true
# ENABLE_JSON_EXPORT=true
# ENABLE_CHARTS=true

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO

# Log file location (inside container)
# LOG_FILE=/app/logs/wren-app.log

# =============================================================================
# Quick Start
# =============================================================================
# 1. Copy this file to .env:      cp .env.example .env
# 2. Add your ANTHROPIC_API_KEY (the only API key you need!)
# 3. Run: docker-compose up -d
# 4. Wait ~5 minutes for services to initialize
#    - Ollama will download nomic-embed-text (~270MB) on first run
#    - Qdrant vector database will initialize
#    - Schema will be auto-embedded on first app startup
# 5. Open: http://localhost:8501
# =============================================================================
